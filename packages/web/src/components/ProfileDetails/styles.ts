import styled from 'styled-components'

const Name = styled.p`
  padding: 0;
  margin: 0;

  font-style: normal;
  font-weight: 500;
  font-size: 24px;

  line-height: 29px;
  letter-spacing: 1.5px;
`

const Email = styled.p`
  padding: 0;
  margin: 0;

  font-size: 14px;

  line-height: 25px;
  letter-spacing: 0.2px;
`

export { Name, Email }
