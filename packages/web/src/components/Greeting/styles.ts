import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
  align-items: center;

  margin-bottom: 35px;
`

const Text = styled.p`
  margin: 0;
  padding: 0;

  font-style: normal;
  font-weight: normal;

  font-size: 24px;
  line-height: 29px;

  color: #54565a;

  b {
    font-weight: 600;
    color: #ff6978;
  }
`

export { Wrapper, Text }
